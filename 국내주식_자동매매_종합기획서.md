# 국내 주식 자동매매 시스템 종합 기획서

> **대상 시장**: KOSPI / KOSDAQ  
> **적용 API**: 키움증권 REST API / 한국투자증권 KIS API  
> **핵심 엔진**: 퀀트 시스템(44전략) + OpenClaw 에이전트 + 래리 윌리엄스 변동성 돌파 + 멀티 에이전트 자기학습  
> **설계 원칙**: 최소 운용 비용 — 목표 월 **₩0 ~ ₩15,000** (무료 티어 최대 활용)  
> **작성일**: 2026년 5월 7일

---

## 목차

1. [프로젝트 개요 및 목표](#1-프로젝트-개요-및-목표)
2. [최소 비용 설계 원칙 및 스택 전체 지도](#2-최소-비용-설계-원칙-및-스택-전체-지도) ⭐ NEW
3. [하드웨어 환경 선택 가이드](#3-하드웨어-환경-선택-가이드) ⭐ NEW
4. [무료·저비용 API 및 서비스 총람](#4-무료저비용-api-및-서비스-총람) ⭐ NEW
5. [AI 모델 비용 최적화 전략](#5-ai-모델-비용-최적화-전략) ⭐ NEW
6. [전체 시스템 아키텍처](#6-전체-시스템-아키텍처)
7. [전략 엔진 상세 설계](#7-전략-엔진-상세-설계)
   - 7-1. 퀀트 스탁 옵티마이저 (44전략 자동 테스트)
   - 7-2. 래리 윌리엄스 변동성 돌파 전략
   - 7-3. OpenClaw 에이전트 실행 레이어
   - 7-4. 멀티 에이전트 자기학습 루프
8. [기술 지표 파라미터 설계](#8-기술-지표-파라미터-설계)
9. [데이터 파이프라인 설계](#9-데이터-파이프라인-설계)
10. [브로커리지 API 연동 설계](#10-브로커리지-api-연동-설계)
11. [스케줄링 및 자동화 설계](#11-스케줄링-및-자동화-설계)
12. [리스크 관리 및 보안 설계](#12-리스크-관리-및-보안-설계)
13. [멀티 에이전트 팀 구성](#13-멀티-에이전트-팀-구성)
14. [구현 로드맵 및 단계별 체크리스트](#14-구현-로드맵-및-단계별-체크리스트)
15. [비용 구조 최종 정리 및 수익 목표](#15-비용-구조-최종-정리-및-수익-목표)
16. [핵심 원칙 및 주의사항](#16-핵심-원칙-및-주의사항)

---

## 2. 최소 비용 설계 원칙 및 스택 전체 지도

### 2-1. 설계 철학: "0원 파이프라인"

모든 구성 요소를 **무료 티어 우선**으로 선택하고, 유료 전환은 수익이 비용을 초과한 시점에만 한다.

```
기본 원칙:
  1. 무료로 가능한 것은 절대 돈 쓰지 않는다
  2. AI 모델은 역할에 따라 분리한다 (고가 모델 = 핵심 판단만)
  3. 서버는 VPS $5/월 또는 집에 있는 기기로 대체한다
  4. 유료 API는 무료 대안이 없을 때만 사용한다
  5. 지출 한도(Spend Limit)를 반드시 설정한다 ($10/월 캡)
```

### 2-2. 최소 비용 스택 전체 지도

```
┌────────────────────────────────────────────────────────────────────┐
│                      전체 비용 목표: 월 ₩0 ~ ₩15,000              │
├──────────────┬─────────────────────────────────┬───────────────────┤
│ 구성 요소     │ 선택                             │ 비용              │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 서버/하드웨어 │ 구형 노트북 / Zeber VPS          │ ₩0 (전기세 소액)  │
│              │ → 유료 필요 시: Hetzner VPS       │ ~$4/월 (₩5,500) │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ AI 모델      │ Ollama 로컬 (Gemma4, Llama3.2)   │ ₩0               │
│ (모니터링)   │ → 대안: OpenRouter 무료 모델      │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ AI 모델      │ Claude Haiku (저가)               │ ~$3/월 (₩4,000) │
│ (분석·판단)  │ → 대안: Gemini Flash 무료 20건/일 │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ AI 모델      │ Claude Sonnet or Opus             │ $0~$5/월 (월 1회) │
│ (월간 최적화)│ → 대안: ChatGPT Plus 구독 활용    │ 기존 구독 활용    │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 주식 데이터  │ pykrx (국내 무료)                 │ ₩0               │
│              │ yfinance (해외 무료)               │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 공시 데이터  │ OpenDART API (무료)               │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 뉴스·검색    │ Tavily API (1,000회/월 무료)      │ ₩0               │
│              │ Brave Search API (1,000건 무료)   │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 데이터베이스 │ Supabase 무료 티어 (500MB)        │ ₩0               │
│              │ → 경량 대안: SQLite (로컬)         │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 대시보드     │ Streamlit (오픈소스)              │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 알림         │ 텔레그램 봇 (무료)               │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 보안 터널    │ Tailscale (무료 Mesh VPN)         │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│ 증권사 API   │ 한국투자증권 KIS API (무료)       │ ₩0               │
│              │ 키움증권 REST API (무료)           │ ₩0               │
├──────────────┼─────────────────────────────────┼───────────────────┤
│              │ 합계 (최저)                       │ ₩0/월            │
│              │ 합계 (현실적 권장)                │ ~₩10,000/월      │
└──────────────┴─────────────────────────────────┴───────────────────┘
```

---

## 3. 하드웨어 환경 선택 가이드

### 3-1. 하드웨어 옵션 비교

| 옵션 | 초기 비용 | 월 비용 | 성능 | 보안 | 추천 대상 |
|------|---------|--------|------|------|---------|
| **A. 구형 노트북 (집)** | ₩0 | 전기세 ₩2,000~₩5,000 | 중 | 낮음 | 완전 입문 |
| **B. 라즈베리 파이 5** | ₩80,000~₩120,000 | 전기세 ₩1,000 | 중 | 중 | 저비용 전용 서버 |
| **C. Zeber VPS (무료)** | ₩0 | ₩0 (크레딧) | 중 | 높음 | **최우선 추천** |
| **D. Hetzner VPS** | ₩0 | ~$4/월 (₩5,500) | 중상 | 높음 | 안정성 우선 |
| **E. DigitalOcean VPS** | ₩0 | $6/월 (₩8,000) | 중상 | 높음 | OpenClaw 원클릭 설치 |
| **F. Mac Mini M4** | ₩600,000~ | 전기세 ₩3,000 | 최고 | 중 | Ollama 고성능 필요 시 |

### 3-2. 옵션별 상세 가이드

#### 옵션 A — 구형 노트북 (비용 ₩0, 입문용)

```
권장 사양:
  CPU: Intel i3 이상 / Apple M1 이상
  RAM: 8GB 이상 (Ollama 로컬 모델 구동 시)
  저장소: SSD 50GB 이상

장점:
  - 즉시 시작 가능, 추가 비용 없음
  - Ollama 로컬 LLM 구동 가능

단점:
  - 노트북 닫으면 시스템 중단 (뚜껑 덮기 설정 변경 필요)
  - 개인 파일과 같은 환경 → 보안 취약

설정:
  macOS: 시스템 설정 → 배터리 → 전원 어댑터 → "화면 끄기 후 절전 방지" 활성화
  Ubuntu: sudo systemctl mask sleep.target hibernate.target
```

#### 옵션 B — 라즈베리 파이 5 (초기 ₩100,000, 전용 서버)

```
권장 구성:
  라즈베리 파이 5 (8GB RAM): ₩95,000
  MicroSD 64GB: ₩15,000
  공식 전원 어댑터: ₩12,000
  케이스: ₩8,000
  합계: 약 ₩130,000 (1회성)

장점:
  - 전력 소비 5~10W → 월 전기세 ₩1,000 수준
  - 24/7 전용 서버로 완전 분리 운용
  - 소형, 조용, 안정적

단점:
  - Ollama 대형 모델 구동 불가 (RAM 한계)
  - Gemma2 2B, Llama3.2 3B 수준 로컬 모델만 가능

OS 설정:
  Raspberry Pi OS (64-bit) Lite 버전 설치
  OpenClaw: Docker 컨테이너로 설치 권장
```

#### 옵션 C — Zeber VPS (월 ₩0, 최우선 추천) ⭐

```
특징:
  - 매월 $5 무료 크레딧 자동 제공
  - Ubuntu 22.04 LTS 선택
  - 1 vCPU, 1GB RAM (기본 플랜)

가입 방법:
  1. zeber.cloud 접속 → 회원가입
  2. "Create VPS" → Ubuntu 22.04 선택
  3. $5 크레딧 자동 적용 → 월 $0

한계:
  - RAM 1GB → Ollama 로컬 모델 구동 불가
  - AI 모델은 외부 API(Haiku, Gemini Flash) 사용 필요
  - 트래픽 제한 있으므로 과도한 크롤링 주의

보완책:
  - 단순 모니터링/스케줄링만 VPS에서 실행
  - 무거운 백테스트는 로컬 PC에서 별도 실행
```

#### 옵션 D — Hetzner VPS (월 ~$4, 안정성 우선)

```
플랜: CX11 (1 vCPU, 2GB RAM, 20GB SSD) → €3.79/월 (~$4)
데이터센터: 핀란드 (지연시간 낮음, 한국 09:00 = 핀란드 02:00)

설치 명령어 (Ubuntu 22.04):
  # OpenClaw 설치
  curl -fsSL https://raw.githubusercontent.com/openclaw/openclaw/main/install.sh | bash

  # Tailscale 보안 터널 설치
  curl -fsSL https://tailscale.com/install.sh | sh
  sudo tailscale up

장점:
  - RAM 2GB → 경량 Ollama 모델 구동 가능 (Gemma2 2B)
  - 99.9% 업타임 SLA 보장
  - 월 $4로 1년 운용 시 $48 = 수익 1~2거래로 회수 가능
```

### 3-3. 권장 하드웨어 선택 플로우차트

```
Q1. 집에 쓰지 않는 노트북/PC가 있나?
  YES → 옵션 A (구형 노트북)로 시작, 안정화 후 VPS 이전
  NO  → Q2로

Q2. 초기 투자 없이 완전 무료로 시작하고 싶나?
  YES → 옵션 C (Zeber VPS 무료 크레딧) 선택
  NO  → Q3으로

Q3. 로컬 LLM(Ollama)을 써서 AI 비용을 0으로 만들고 싶나?
  YES + 예산 ₩13만 → 옵션 B (라즈베리 파이 5)
  YES + 예산 ₩60만↑ → 옵션 F (Mac Mini M4)
  NO  → 옵션 D (Hetzner $4/월) 또는 E (DigitalOcean $6/월)
```

---

## 4. 무료·저비용 API 및 서비스 총람

### 4-1. 증권사 API (완전 무료)

| 증권사 | API 종류 | 무료 여부 | 모의투자 | 특징 |
|--------|---------|---------|---------|------|
| **한국투자증권** | KIS OpenAPI (REST) | 완전 무료 | ✅ 지원 | 문서 충실, Python 예제 풍부 |
| **키움증권** | OpenAPI+ (REST) | 완전 무료 | ✅ 지원 | 국내 1위 점유율, 데이터 풍부 |

```
한국투자증권 KIS API 발급 절차:
  1. https://apiportal.koreainvestment.com 접속
  2. 회원가입 → API Key 신청
  3. App Key + App Secret 발급 (즉시)
  4. 모의투자 URL: https://openapivts.koreainvestment.com:29443
  5. 실전투자 URL: https://openapi.koreainvestment.com:9443

키움증권 REST API 발급 절차:
  1. 영웅문 글로벌 앱 설치 → 계좌 개설
  2. https://openapi.kiwoom.com 접속 → 앱 등록
  3. App Key + Secret Key 발급
  4. 모의투자 계좌로 먼저 테스트
```

### 4-2. 시장 데이터 (완전 무료)

| 도구/API | 제공 데이터 | 비용 | 설치 |
|---------|-----------|------|------|
| **pykrx** | KOSPI/KOSDAQ OHLCV, 시총, 투자자별 매매 | 무료 | `pip install pykrx` |
| **yfinance** | 해외 주식 OHLCV, 재무 데이터 | 무료 | `pip install yfinance` |
| **FinanceDataReader** | 국내외 주식, 환율, 원자재 | 무료 | `pip install finance-datareader` |
| **OpenDART API** | 기업 재무제표, 공시 전문 | 무료 (회원가입) | REST API |

```python
# pykrx 사용 예시 — 삼성전자 1년치 OHLCV 수집
from pykrx import stock
import pandas as pd

df = stock.get_market_ohlcv_by_date("20250101", "20260101", "005930")
print(df.head())

# FinanceDataReader — KOSPI 200 전 종목 목록
import FinanceDataReader as fdr
krx = fdr.StockListing('KOSPI')
print(krx[['Code', 'Name', 'Market']].head(20))
```

### 4-3. 공시 및 뉴스 (무료 티어 충분)

| 서비스 | 무료 한도 | 용도 | 발급 방법 |
|--------|---------|------|---------|
| **OpenDART API** | 제한 없음 (공개 데이터) | 재무제표, 공시 전문 | dart.fss.or.kr → API 키 신청 |
| **Tavily API** | 1,000건/월 무료 | AI 최적화 웹 검색, 뉴스 | tavily.com → 회원가입 |
| **Brave Search API** | 1,000건/월 무료 | 독립 검색 엔진, 추적 없음 | api.search.brave.com |
| **네이버 금융** | 무제한 (크롤링) | 종목 뉴스, 토론 감성 | Beautiful Soup 크롤링 |

```python
# OpenDART — 삼성전자 최근 사업보고서 수집
import requests

url = "https://opendart.fss.or.kr/api/fnlttSinglAcntAll.json"
params = {
    "crtfc_key": "YOUR_DART_API_KEY",
    "corp_code": "00126380",   # 삼성전자 고유번호
    "bsns_year": "2025",
    "reprt_code": "11011"      # 사업보고서
}
response = requests.get(url, params=params)
data = response.json()
```

### 4-4. 데이터베이스 (완전 무료)

| 옵션 | 무료 한도 | 장점 | 단점 | 추천 상황 |
|------|---------|------|------|---------|
| **SQLite (로컬)** | 무제한 | 설치 불필요, 빠름 | 서버 공유 불가 | VPS 단독 운용 |
| **Supabase** | 500MB, 프로젝트 2개 | 클라우드, REST API 제공 | 500MB 한도 | 다기기 접근 필요 시 |
| **TinyDB** | 무제한 | JSON 파일 기반, 초경량 | 대용량 부적합 | 초소형 시스템 |

```python
# SQLite — 거래 기록 저장 (설치 불필요, Python 내장)
import sqlite3

conn = sqlite3.connect('/data/trading.db')
cursor = conn.cursor()

cursor.execute('''
    CREATE TABLE IF NOT EXISTS trades (
        id INTEGER PRIMARY KEY,
        ticker TEXT,
        side TEXT,
        price REAL,
        quantity INTEGER,
        pnl REAL,
        executed_at TEXT
    )
''')
conn.commit()
```

### 4-5. AI 모델 무료·저가 옵션

| 모델 | 비용 | 성능 | 용도 | 접근 방법 |
|------|------|------|------|---------|
| **Ollama + Gemma4 (로컬)** | ₩0 | 중 | 상시 모니터링 | `ollama run gemma4` |
| **Ollama + Llama3.2 3B (로컬)** | ₩0 | 중 | 경량 분석 | `ollama run llama3.2:3b` |
| **Gemini 2.0 Flash** | 무료 20건/일 | 중상 | 일일 분석 | Google AI Studio |
| **OpenRouter 무료 모델** | ₩0 | 중 | 보조 분석 | openrouter.ai |
| **Claude Haiku 4.5** | $0.80/백만 토큰 | 중상 | 저가 유료 옵션 | Anthropic API |
| **DeepSeek V3** | 초저가 (1/10~1/20) | 중상~고 | 비용 절감 유료 | deepseek.com |
| **Claude Sonnet 4.6** | $3/백만 토큰 | 고 | 월간 전략 최적화 | Anthropic API |

### 4-6. 보안·인프라 (완전 무료)

| 서비스 | 용도 | 비용 |
|--------|------|------|
| **Tailscale** | VPN Mesh 보안 터널 (포트 미노출) | 무료 (3기기까지) |
| **Cloudflare Tunnel** | 도메인 없이 로컬 서비스 외부 노출 | 무료 |
| **텔레그램 봇** | 실시간 매매 알림, 원격 명령 | 무료 |
| **GitHub Actions** | 백테스트 자동화 CI/CD | 무료 (월 2,000분) |

---

## 5. AI 모델 비용 최적화 전략

### 5-1. 역할별 모델 라우팅 (두뇌 vs 근육 분리)

```
                    [오케스트레이터]
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
   [단순 작업]     [분석 작업]    [전략 작업]
   "근육 모델"    "중간 모델"    "두뇌 모델"
   Ollama 로컬    Claude Haiku   Claude Sonnet
   or Gemini      or DeepSeek    (월 1~2회만)
   Flash (무료)   (초저가)
   ────────────   ──────────────  ────────────
   시세 확인      일일 손익 분석  월간 전략
   손절 모니터    뉴스 감성 분석  재최적화
   크론잡 실행    공시 요약       새 종목 발굴
   로그 기록      보고서 생성
   비용: ₩0       비용: ~₩3,000  비용: ~₩5,000
```

### 5-2. 토큰 비용 폭탄 방지 규칙

```
규칙 1 — 하트비트 주기 최적화
  기본 10분 → 30분으로 늘리면 비용 67% 절감
  기본 10분 → 60분으로 늘리면 비용 83% 절감
  (장 마감 후 야간은 60분 또는 중단)

규칙 2 — 컨텍스트 정리 주기
  OpenClaw: 매일 장 마감 후 /compact 실행
  누적 컨텍스트가 길어지면 토큰 비용 기하급수 증가

규칙 3 — API 지출 한도 설정 (필수)
  Anthropic 대시보드: Settings → Billing → Spend Limit
  월 $5~$10 한도 설정 → 무한 루프 오류 시 자동 차단

규칙 4 — 무료 모델 우선 활용
  단순 판단(매수/매도/대기) → Ollama 로컬 모델
  복잡한 분석 → Claude Haiku (저가)
  전략 설계 → Claude Sonnet or Opus (월 1~2회)

규칙 5 — 프롬프트 캐싱 활용
  Claude API: 동일한 시스템 프롬프트 반복 사용 시
  Prompt Caching으로 90% 비용 절감 가능
```

### 5-3. Ollama 로컬 LLM 설치 및 OpenClaw 연동

```bash
# 1. Ollama 설치 (Mac/Linux)
curl -fsSL https://ollama.ai/install.sh | sh

# 2. 추천 모델 다운로드 (RAM에 따라 선택)
ollama pull gemma4          # 4GB RAM 필요 — 권장
ollama pull llama3.2:3b     # 2GB RAM 필요 — 경량
ollama pull qwen2.5:7b      # 8GB RAM 필요 — 고성능

# 3. OpenClaw에서 Ollama 연동
# openclaw.json 수정:
{
  "model": "ollama/gemma4",
  "base_url": "http://localhost:11434",
  "api_key": "ollama"       # 로컬이므로 키 불필요
}

# 4. 고성능 작업만 Claude API 사용하도록 분기
# agents.md에 명시:
# "단순 시세 확인, 손절 체크는 로컬 모델 사용
#  손익 분석, 전략 판단은 Claude Haiku 사용
#  월간 재최적화는 Claude Sonnet 사용"
```

---

---

## 1. 프로젝트 개요 및 목표

### 1-1. 프로젝트 배경

키움증권과 한국투자증권이 **REST API**를 제공하면서 과거 Windows OCX 전용 자동매매 시대가 종료됐다. 이제 Linux/Mac 클라우드 환경에서 AI 에이전트와 결합한 풀스택 자동매매 시스템 구축이 가능하다.

본 시스템은 4가지 검증된 접근 방식을 **단일 파이프라인**으로 통합한다:

| 레이어 | 역할 | 핵심 방법론 |
|--------|------|------------|
| 전략 발굴 | 최적 전략·종목 탐색 | 퀀트 44전략 그리드 서치 백테스트 |
| 진입 타이밍 | 정밀 매수 트리거 | 래리 윌리엄스 변동성 돌파 + ATR |
| 실행 자동화 | 24/7 무인 운용 | OpenClaw 에이전트 + 크론잡 |
| 지속 진화 | 자기학습 고도화 | 멀티 에이전트 피드백 루프 |

### 1-2. 정량적 목표

| 항목 | 목표치 |
|------|--------|
| 연간 수익률 | KOSPI 지수 대비 +10% 이상 초과 달성 |
| 월간 승률 | 55% 이상 |
| 최대 낙폭(MDD) | -15% 이하 |
| 샤프 지수(Sharpe Ratio) | 1.5 이상 |
| 일일 손실 한도(Circuit Breaker) | -3% 초과 시 당일 매매 중단 |
| 시스템 가동률 | 장 중 99% 이상 (09:00~15:30) |

### 1-3. 운용 자금 구조 (예시)

```
총 투자 자금: 1,000만 원

├── 실제 자동매매 운용: 700만 원 (70%)
│   ├── 슬롯 1: 140만 원 (전략 A — 퀀트 추세 추종)
│   ├── 슬롯 2: 140만 원 (전략 B — 퀀트 평균 회귀)
│   ├── 슬롯 3: 140만 원 (전략 C — 변동성 돌파)
│   ├── 슬롯 4: 140만 원 (전략 D — 멀티팩터 스코어링)
│   └── 슬롯 5: 140만 원 (전략 E — 공시/뉴스 이벤트)
│
└── 비상 예비금: 300만 원 (30%) — 손실 한도 초과 시 시스템 즉시 중단
```

---

## 6. 전체 시스템 아키텍처

### 6-1. 4계층 아키텍처 (Four-Layer Architecture)

```
┌─────────────────────────────────────────────────────────────────┐
│                     LAYER 4: FEEDBACK & LEARNING                │
│  손익 분석 보고서 → AI 피드백 → 파라미터 재조정 → 전략 재등록   │
│  (Supabase DB ← 거래 결과 → Claude Opus 분석 → 전략 업데이트)  │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                      LAYER 3: EXECUTION                          │
│  OpenClaw 에이전트 (VPS 24/7)                                    │
│  ├── 크론잡 스케줄러 (09:00~15:30 장중 / 장외 분석)             │
│  ├── 키움증권 REST API 주문 실행 모듈                            │
│  └── 한국투자증권 KIS API 주문 실행 모듈                         │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                    LAYER 2: STRATEGY & SCORING                   │
│  ① 퀀트 스탁 옵티마이저 (44전략 그리드 서치 백테스트)           │
│  ② 래리 윌리엄스 변동성 돌파 (14분 레인지 + ATR 자금관리)       │
│  ③ 멀티팩터 스코어링 (가격변동성 + 수급 + 시계열 패턴)          │
│  ④ 감성 분석 (뉴스 + OpenDART 공시 텍스트 → 점수화)            │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                       LAYER 1: DATA                              │
│  pykrx (국내 OHLCV) │ OpenDART API (공시) │ 뉴스 API (감성)    │
│  실시간 시세 (5분봉) │ Supabase PostgreSQL (거래 기록 저장)     │
└─────────────────────────────────────────────────────────────────┘
```

### 6-2. 운용 인프라 구성

```
[OpenClaw VPS 서버]
  OS: Ubuntu 22.04 LTS
  RAM: 2GB 이상 (VPS $10~$20/월)
  역할: 24/7 에이전트 상시 가동, 크론잡 실행, API 호출

[로컬 개발 환경]
  MacBook / Windows PC
  역할: 전략 개발, 백테스트, OpenClaw 원격 명령

[클라우드 DB]
  Supabase (PostgreSQL, 무료 플랜 가능)
  역할: 거래 기록, 포트폴리오 스냅샷, 전략 파라미터 저장

[알림 채널]
  텔레그램 봇: 실시간 매수/매도 알림, 손익 현황
  (선택) ClickUp: 일일 보고서 자동 생성
```

---

## 7. 전략 엔진 상세 설계

### 7-1. 퀀트 스탁 옵티마이저 (44전략 자동 테스트)

#### 개요
약 44개의 기술적 분석 전략을 전 종목에 대해 자동으로 백테스트하고, **샤프 지수 우선 원칙**으로 종목별 최적 전략·파라미터 조합을 도출하는 엔진.

#### 테스트 전략 카테고리

| 카테고리 | 전략 수 | 대표 전략 |
|---------|--------|----------|
| 추세 추종 (Trend Following) | 12개 | SMA 골든크로스, EMA 크로스, MACD 시그널, ADX 추세 강도 |
| 평균 회귀 (Mean Reversion) | 10개 | RSI 과매도, 볼린저 밴드 하단, 스토캐스틱 K/D, CCI |
| 모멘텀 (Momentum) | 8개 | 가격 모멘텀, ROC(변화율), 상대 모멘텀, 52주 신고가 |
| 변동성 (Volatility) | 7개 | 볼린저 밴드 수축, ATR 돌파, 변동성 돌파, Keltner Channel |
| 거래량 (Volume) | 7개 | VWAP 이탈, OBV 추세, 거래량 급증 필터, MFI |

#### 종목별 최적화 프로세스

```
STEP 1 — 유동성 1차 필터
  └── KOSPI/KOSDAQ 전 종목 중 일평균 거래대금 상위 100종목 선정
  └── 상장 2년 미만, 관리종목, 거래정지 종목 제외

STEP 2 — 과거 10년 데이터 수집 (pykrx 활용)
  └── 일봉 OHLCV 데이터 수집
  └── 결측치·이상치 처리

STEP 3 — 그리드 서치 (Grid Search) 파라미터 최적화
  └── 44개 전략 × 각 전략별 파라미터 범위 전수 테스트
  └── 예) RSI: 기간 [7, 10, 14, 21], 과매도 [20, 25, 30, 35]
  └── 조합 수: 전략 44개 × 평균 20개 파라미터 조합 = 880회/종목

STEP 4 — 성과 평가 (샤프 지수 우선)
  └── 평가 기준:
      ① 샤프 지수 ≥ 1.5 (필수)
      ② MDD ≤ -15%
      ③ 연간 수익률 > KOSPI 지수
      ④ 승률 ≥ 50%
  └── 4개 기준 모두 충족한 [종목 × 전략 × 파라미터] 조합 저장

STEP 5 — 종목별 Best Strategy 등록
  └── 예시 결과:
      삼성전자:  SMA 크로스 (20/100일), 샤프 1.8, MDD -11%
      SK하이닉스: RSI 평균회귀 (21일, 과매도 25), 샤프 1.6, MDD -13%
      LG화학:     RSI 평균회귀 (14일, 과매도 20), 샤프 1.7, MDD -12%
      카카오:     볼린저밴드 + 거래량 (20일, 2σ), 샤프 1.5, MDD -14%
```

#### 재최적화 주기
- **월 1회** 전체 그리드 서치 재실행 (시장 구조 변화 반영)
- 특정 종목의 샤프 지수가 **1.0 이하로 3주 연속 유지** 시 즉시 해당 종목 교체

---

### 7-2. 래리 윌리엄스 변동성 돌파 전략

#### 전략 철학
장 개시 직후 14분간 형성되는 가격 레인지를 기준으로, **그 레인지를 상향 돌파하는 순간**을 강한 당일 모멘텀 신호로 해석하고 즉시 매수에 진입. 장 마감 전 전량 청산하여 오버나잇 리스크 제거.

#### 매매 로직 상세

```
[매수 조건]
  트리거가 = 당일 시가 + (장 개시 14분간 레인지 × K계수)

  여기서:
  - 14분 레인지 = 09:00~09:14 구간의 (고가 - 저가)
  - K계수 = 0.5 (기본값, 종목 변동성에 따라 0.3~0.7 조정)

  → 현재가 ≥ 트리거가 AND 거래량 조건 충족 → 즉시 시장가 매수

[ATR 기반 포지션 사이징]
  - ATR(14일) ≥ 3%  → 총 자본의 5% 투입 (고변동성 보수 대응)
  - ATR(14일) 1.5~3% → 총 자본의 10% 투입
  - ATR(14일) 0.5~1.5% → 총 자본의 20% 투입 (저변동성 적극 대응)
  - ATR(14일) < 0.5%  → 매수 보류 (유동성 부족 가능성)

[청산 조건 — 우선순위 순]
  1. 익절: 매수가 대비 +3% 이상 도달 시 즉시 시장가 청산
  2. 손절: 매수가 대비 -2% 하락 시 즉시 시장가 청산 (손익비 1.5)
  3. 시간 청산: 위 조건 미충족 시 14:50에 전량 일괄 시장가 매도
      → 오버나잇 리스크 Zero 전략 유지

[매수 제외 필터]
  ├── 갭 상승 3% 이상 종목 (이미 레인지 돌파 과열)
  ├── 상한가 직전 종목 (슬리피지 과다 위험)
  ├── 거래대금 10억 미만 종목 (유동성 부족)
  └── 공시 발표 당일 종목 (이벤트 변동성 회피)
```

#### Streamlit 모니터링 대시보드

```
화면 구성:
  ┌────────────────────────────────────┐
  │ 종목    현재가   트리거가  ATR   비중  │
  │ 005930  78,500   79,200  1.8%  10%  │  ← 대기
  │ 000660  198,000  195,500  2.1%  10%  │  ← 돌파 (매수 실행)
  │ 035420  52,400   54,000  3.2%   5%  │  ← 대기
  └────────────────────────────────────┘
  오늘 손익: +142,000원 (+0.71%)
  보유 슬롯: 2/5 사용 중
```

---

### 7-3. OpenClaw 에이전트 실행 레이어

#### 에이전트 설정 파일 구조

```
/openclaw-workspace/
├── agents.md          ← 에이전트 동작 규칙 및 권한 범위
├── soul.md            ← 에이전트 성격·원칙 (보수적/공격적 성향 조정)
├── tools.md           ← 사용 가능한 도구 목록 (API 호출, DB 조회)
├── memory.md          ← 과거 거래 기억, 전략 히스토리
└── workspace/
    ├── trading/
    │   ├── skills/
    │   │   ├── kiwoom_api.py      ← 키움증권 REST API 래퍼
    │   │   ├── kis_api.py         ← 한국투자증권 KIS API 래퍼
    │   │   ├── data_pipeline.py   ← pykrx / OpenDART 데이터 수집
    │   │   ├── quant_engine.py    ← 44전략 백테스트 엔진
    │   │   ├── larry_williams.py  ← 변동성 돌파 전략
    │   │   └── scoring.py         ← 멀티팩터 스코어링
    │   └── data/
    │       ├── market_data.db     ← OHLCV 로컬 캐시
    │       └── portfolio.json     ← 현재 포트폴리오 상태
    └── reports/                   ← 일일 손익 보고서 자동 저장
```

#### agents.md 핵심 지침 (예시)

```markdown
## 운영 원칙
- 실계좌 매매는 반드시 DRY_RUN=False 확인 후만 실행
- 매수 가능 최대 금액: 총 자산의 50% (MAX_POSITION_RATIO=0.5)
- 일일 손실 한도: -3% 초과 시 당일 즉시 전량 청산 후 매매 중단
- 출금(Withdrawal) 관련 API 호출 절대 금지

## 자율 작동 메커니즘
- 크론잡: 오전 8:30 시장 분석 → 09:05 변동성 돌파 모니터링 시작
          → 10:00 스코어링 기반 종목 매수 → 14:50 전량 청산
- 하트비트: 10분마다 포트폴리오 상태 점검, 손절 조건 확인
- 외프기(Webhook): 텔레그램 명령 수신 시 즉시 반응

## 보고 의무
- 매 거래 완료 시 텔레그램으로 체결 내역 즉시 발송
- 장 마감 후 일일 손익 보고서 자동 생성 및 발송
```

---

### 7-4. 멀티 에이전트 자기학습 루프

#### 자기학습 사이클 (Daily Feedback Loop)

```
매일 15:30 장 마감 후:

┌─────────────────────────────────────────────────────┐
│ ① 거래 결과 수집 (Execution Agent)                   │
│    └── 오늘 체결 내역, 손익, 승률 → Supabase 저장   │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│ ② 손익 분석 (Analysis Agent — Claude Opus)          │
│    └── 손실 종목 원인 분석 (팩터 편향, 슬리피지 등) │
│    └── 수익 종목 패턴 강화 요소 추출               │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│ ③ 파라미터 조정 제안 (Strategy Agent)               │
│    └── 손실 전략의 K계수, ATR 임계값, RSI 기간 조정│
│    └── 슬롯 비중 재배분 (수익 전략 ↑, 손실 ↓)     │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│ ④ 보고서 생성 (Reporter Agent — Claude Haiku)       │
│    └── 일일 손익 보고서 → 텔레그램/이메일 발송      │
│    └── memory.md에 오늘 학습 내용 추가 저장         │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│ ⑤ 전략 재등록 (Orchestrator Agent)                  │
│    └── 수정된 파라미터로 크론잡 재설정              │
│    └── 내일 장 시작 전 최종 설정 적용 완료          │
└─────────────────────────────────────────────────────┘
```

---

## 8. 기술 지표 파라미터 설계

### 4-1. 국내 주요 종목별 최적화 파라미터 (백테스트 기준)

| 종목 | 전략 | 지표 | 최적 파라미터 | 샤프 지수 | MDD |
|------|------|------|-------------|----------|-----|
| 삼성전자 (005930) | 추세 추종 | SMA 크로스 | 단기 20일 / 장기 100일 | 1.8 | -11% |
| SK하이닉스 (000660) | 평균 회귀 | RSI | 기간 21일, 과매도 25 / 과매수 75 | 1.6 | -13% |
| LG화학 (051910) | 평균 회귀 | RSI | 기간 14일, 과매도 20 / 과매수 80 | 1.7 | -12% |
| 카카오 (035720) | 혼합 | 볼린저+거래량 | 기간 20일, 2.0σ | 1.5 | -14% |
| 현대차 (005380) | 추세 추종 | MACD | 단기 12 / 장기 26 / 시그널 9 | 1.6 | -13% |
| NAVER (035420) | 평균 회귀 | 볼린저 밴드 | 기간 15일, 1.8σ | 1.7 | -11% |

> **주의**: 위 파라미터는 과거 데이터 기준이며, 매월 재최적화 필수

### 4-2. 지표별 매매 로직 상세

#### ① RSI (상대강도지수)

```python
# 매수 조건 (평균 회귀)
if rsi < oversold_threshold:          # 과매도 (종목별 20~35)
    if price <= bollinger_lower:       # 볼린저 밴드 하단 확인
        signal = "BUY"

# 과매도 + 볼린저 하단 + 공포탐욕지수 ≤ 30 → 3중 확인 매수 (강력 신호)
if rsi < 30 and price <= bollinger_lower and fgi <= 30:
    position_size *= 1.5              # 강력 신호 → 비중 50% 증가

# 청산 조건
if rsi > overbought_threshold:        # 과매수 (종목별 65~80)
    signal = "SELL"
```

#### ② 볼린저 밴드 (Bollinger Bands)

```python
# 파라미터 범위 (그리드 서치 대상)
periods = [15, 18, 20, 22, 25]
std_multipliers = [1.5, 1.8, 2.0, 2.2, 2.5]

# 매수: 하단 터치 + 거래량 확인 (이전 5일 평균 대비 1.5배 이상)
if price <= lower_band and volume >= avg_volume * 1.5:
    signal = "BUY"

# 청산: 상단 터치 또는 중간선(중심선) 도달
if price >= upper_band or price >= middle_band:
    signal = "SELL"
```

#### ③ VWAP (거래량 가중 평균 가격)

```python
# 데이트레이딩 진입 필터
# 조건: 현재가 > VWAP, 단 VWAP 대비 이격도 1.5% 이내 (추격 매수 방지)
if price > vwap:
    deviation = (price - vwap) / vwap * 100
    if deviation <= 1.5:             # VWAP 근처에서만 진입
        signal = "BUY"

# 청산: VWAP 하향 이탈
if price < vwap:
    signal = "SELL"
```

#### ④ ATR (Average True Range) 자금 관리

```python
def calculate_position_size(total_capital, atr_pct, slot_ratio=0.2):
    """
    ATR 기반 동적 포지션 사이징
    total_capital: 운용 자금
    atr_pct: ATR / 현재가 × 100 (백분율)
    """
    if atr_pct >= 3.0:
        return total_capital * 0.05   # 고변동성: 자본의 5%
    elif atr_pct >= 1.5:
        return total_capital * 0.10   # 중변동성: 자본의 10%
    elif atr_pct >= 0.5:
        return total_capital * 0.20   # 저변동성: 자본의 20%
    else:
        return 0                      # 매수 보류 (유동성 부족)
```

#### ⑤ 래리 윌리엄스 14분 레인지 돌파

```python
def larry_williams_trigger(open_price, high_14min, low_14min, k=0.5):
    """
    장 개시 후 14분간의 레인지 × K계수로 매수 임계가 산출
    """
    range_14min = high_14min - low_14min
    trigger = open_price + (range_14min * k)
    return trigger

# 실시간 모니터링 (09:00~10:00)
if current_price >= trigger and volume_ok:
    atr = calculate_atr(prices, period=14)
    position_size = calculate_position_size(capital, atr_pct)
    execute_market_buy(ticker, position_size)
```

---

## 9. 데이터 파이프라인 설계

### 5-1. 데이터 소스 및 수집 도구

| 데이터 종류 | 소스 | 도구/라이브러리 | 수집 주기 |
|-----------|------|----------------|---------|
| 국내 주식 OHLCV (일봉) | KRX 한국거래소 | `pykrx` | 장 마감 후 1회 |
| 국내 주식 OHLCV (5분봉) | 키움증권 API | `kiwoom REST` | 5분마다 (장중) |
| 재무 정보 / 공시 | 전자공시 DART | `OpenDART API` | 10분마다 (장중) |
| 뉴스 / 감성 분석 | 네이버 금융, 연합뉴스 | `Tavily API` or Crawling | 10분마다 (장중) |
| 공포탐욕지수 | CNN FGI or 자체 산출 | `requests` | 1회/일 (장 시작 전) |
| 미국 시장 데이터 | Yahoo Finance | `yfinance` | 장 시작 전 1회 |

### 5-2. Supabase DB 스키마

```sql
-- 시장 데이터 테이블
CREATE TABLE market_data (
    id          SERIAL PRIMARY KEY,
    ticker      VARCHAR(10) NOT NULL,
    date        DATE NOT NULL,
    open        DECIMAL(12,2),
    high        DECIMAL(12,2),
    low         DECIMAL(12,2),
    close       DECIMAL(12,2),
    volume      BIGINT,
    created_at  TIMESTAMP DEFAULT NOW()
);

-- 전략 파라미터 저장 테이블
CREATE TABLE strategy_params (
    id          SERIAL PRIMARY KEY,
    ticker      VARCHAR(10) NOT NULL,
    strategy    VARCHAR(50) NOT NULL,   -- 예: 'RSI_MeanReversion'
    params      JSONB,                  -- {"period": 21, "oversold": 25, ...}
    sharpe      DECIMAL(6,4),
    mdd         DECIMAL(6,4),
    win_rate    DECIMAL(6,4),
    updated_at  TIMESTAMP DEFAULT NOW()
);

-- 매매 실행 기록 테이블
CREATE TABLE trades (
    id           SERIAL PRIMARY KEY,
    ticker       VARCHAR(10),
    side         VARCHAR(4),            -- 'BUY' or 'SELL'
    price        DECIMAL(12,2),
    quantity     INTEGER,
    amount       DECIMAL(14,2),
    strategy     VARCHAR(50),
    pnl          DECIMAL(12,2),        -- 실현 손익
    pnl_pct      DECIMAL(8,4),        -- 손익률 (%)
    executed_at  TIMESTAMP,
    created_at   TIMESTAMP DEFAULT NOW()
);

-- 포트폴리오 스냅샷 테이블
CREATE TABLE portfolio_snapshots (
    id              SERIAL PRIMARY KEY,
    snapshot_date   DATE,
    total_value     DECIMAL(14,2),
    cash            DECIMAL(14,2),
    positions       JSONB,             -- 보유 종목 목록
    daily_pnl       DECIMAL(12,2),
    cumulative_pnl  DECIMAL(12,2),
    created_at      TIMESTAMP DEFAULT NOW()
);

-- AI 의사결정 로그 테이블
CREATE TABLE ai_decisions (
    id          SERIAL PRIMARY KEY,
    ticker      VARCHAR(10),
    decision    VARCHAR(10),           -- 'BUY', 'SELL', 'HOLD'
    reason      TEXT,                  -- AI 판단 근거
    confidence  DECIMAL(4,2),         -- 신뢰도 0.0~1.0
    model_used  VARCHAR(50),
    created_at  TIMESTAMP DEFAULT NOW()
);
```

### 5-3. 데이터 흐름도

```
[pykrx / 키움API]   [OpenDART]   [뉴스 API]
       │                  │            │
       ▼                  ▼            ▼
   [ETL 전처리 모듈 — data_pipeline.py]
   - 결측치 처리
   - 이상치 필터링
   - 지표 계산 (RSI, MACD, BB, ATR, VWAP)
       │
       ▼
   [Supabase PostgreSQL DB]
   - market_data (OHLCV)
   - strategy_params (최적 파라미터)
   - trades (체결 기록)
       │
       ▼
   [Strategy Layer]
   ├── quant_engine.py → 종목별 최적 전략 선별
   ├── larry_williams.py → 14분 레인지 트리거 계산
   └── scoring.py → 멀티팩터 점수 산출
       │
       ▼
   [Execution Layer — OpenClaw 에이전트]
   ├── kiwoom_api.py → 키움증권 실계좌 매수/매도
   └── kis_api.py → 한국투자증권 실계좌 매수/매도
```

---

## 10. 브로커리지 API 연동 설계

### 6-1. 키움증권 REST API

```python
# 환경변수 설정 (.env 파일 — 절대 코드에 하드코딩 금지)
KIWOOM_APP_KEY=your_app_key_here
KIWOOM_SECRET_KEY=your_secret_key_here
KIWOOM_ACCOUNT_NO=1234567890
KIWOOM_BASE_URL=https://openapi.koreainvestment.com:9443  # 실전
# KIWOOM_BASE_URL=https://openapivts.koreainvestment.com:29443  # 모의

# 주요 API 엔드포인트
class KiwoomAPI:
    def get_access_token(self) -> str:
        """OAuth2 액세스 토큰 발급 (6시간 유효)"""
        ...

    def get_stock_price(self, ticker: str) -> dict:
        """현재가 조회"""
        ...

    def get_ohlcv_daily(self, ticker: str, days: int = 100) -> pd.DataFrame:
        """일봉 OHLCV 조회"""
        ...

    def place_market_buy(self, ticker: str, quantity: int) -> dict:
        """시장가 매수"""
        ...

    def place_market_sell(self, ticker: str, quantity: int) -> dict:
        """시장가 매도"""
        ...

    def get_portfolio(self) -> dict:
        """현재 보유 종목 및 평가 손익 조회"""
        ...
```

### 6-2. 한국투자증권 KIS API

```python
# 환경변수 설정
KIS_APP_KEY=your_app_key_here
KIS_APP_SECRET=your_app_secret_here
KIS_ACCOUNT_NO=50123456-01
KIS_BASE_URL=https://openapi.koreainvestment.com:9443  # 실전투자

# kis_api.py 핵심 메서드
class KISAPI:
    def issue_token(self) -> str:
        """OAuth 토큰 발급"""
        ...

    def get_inquire_price(self, ticker: str) -> dict:
        """주식 현재가 조회 (tr_id: FHKST01010100)"""
        ...

    def order_cash_buy(self, ticker: str, qty: int, price: int = 0) -> dict:
        """현금 매수 주문 (price=0이면 시장가)"""
        ...

    def order_cash_sell(self, ticker: str, qty: int, price: int = 0) -> dict:
        """현금 매도 주문"""
        ...

    def get_balance(self) -> dict:
        """잔고 및 보유종목 조회"""
        ...
```

### 6-3. API 이중화 전략

```
평상시: 키움증권 API 기본 사용
       ↓
API 장애/오류 감지 (연속 3회 타임아웃)
       ↓
자동 전환: 한국투자증권 KIS API로 페일오버
       ↓
텔레그램 알림: "⚠️ 키움 API 장애 → KIS API로 전환"
```

---

## 11. 스케줄링 및 자동화 설계

### 7-1. 전체 일일 스케줄 (크론잡 설계)

```
[장 시작 전 — 08:30~09:00]
  08:30  뉴스 수집 및 감성 점수 갱신 (Tavily API)
  08:40  전일 OHLCV 데이터 Supabase 적재
  08:45  래리 윌리엄스 전략 대상 종목 스크리닝 (50종목 → 10종목)
  08:50  멀티팩터 스코어링 실행 → 상위 5종목 선정
  08:55  당일 ATR 사전 계산 → 슬롯별 투자 비중 확정
  08:59  일일 Circuit Breaker 초기화 (손실 카운터 리셋)

[장 초반 — 09:00~10:00]
  09:00  장 개시, 실시간 시세 수신 시작
  09:05  5분봉 데이터 수집 시작 (5분마다 반복)
  09:14  ★ 14분 레인지 확정 → 래리 윌리엄스 트리거가 계산
  09:15  트리거가 이상 돌파 종목 실시간 모니터링 시작
  09:15~ 트리거 돌파 즉시 시장가 매수 실행 (조건 충족 시)

[장 중반 — 10:00~14:00]
  10:00  ★ 멀티팩터 스코어링 기반 매수 실행 (70점 이상 종목)
  10:00~ 10분마다 공시 정보 모니터링 (OpenDART)
  10:00~ 10분마다 하트비트 — 손절 조건 확인 (-2% 이하 즉시 매도)
  10:00~ 30분마다 포트폴리오 스냅샷 저장

[장 후반 — 14:00~15:30]
  14:00  일일 손실 한도 확인 → -3% 초과 시 전량 청산 후 매매 중단
  14:50  ★ 전 보유 종목 일괄 시장가 청산 (래리 윌리엄스 종목)
  15:00  포지션 최종 정리 확인
  15:30  장 마감

[장 마감 후 — 15:30~18:00]
  15:35  ★ 일일 손익 보고서 생성 (AI 분석 에이전트 실행)
  15:45  손실 종목 원인 분석 → 파라미터 조정 제안
  16:00  ★ memory.md 업데이트 (오늘 학습 내용 저장)
  16:30  내일 전략 설정 반영 (크론잡 재설정)
  17:00  텔레그램 일일 보고서 발송

[야간 / 새벽 — 22:00~07:00]
  22:00  월간 전략 재최적화 실행 (매월 첫째 주 일요일)
  03:00  레거시 데이터 정리 (90일 이상 된 5분봉 데이터 아카이브)
```

### 7-2. OpenClaw 크론잡 설정 예시

```
# OpenClaw 명령어로 스케줄 등록
"오전 8시 30분에 뉴스 수집 및 감성 분석 실행하고 결과를 Supabase에 저장해줘.
 오전 9시 14분에 오늘 개장 이후 14분간의 고가/저가를 확인하여
 래리 윌리엄스 트리거가를 계산하고 모니터링을 시작해줘.
 오후 2시 50분에 보유한 전 종목을 시장가로 일괄 청산해줘.
 오후 3시 35분에 오늘 손익 분석 보고서를 만들어 텔레그램으로 보내줘.
 모든 작업을 크론잡으로 등록해서 매일 자동 실행되게 해줘."
```

---

## 12. 리스크 관리 및 보안 설계

### 8-1. 다층 리스크 관리 체계

```
Level 1 — 종목 단위 리스크
  ├── 종목당 최대 투자 비중: 총 자산의 20% 이하
  ├── 개별 손절: 매수가 대비 -2% 즉시 시장가 청산
  └── 익절: 매수가 대비 +3% 즉시 시장가 청산

Level 2 — 포트폴리오 단위 리스크
  ├── 동시 보유 종목: 최대 5종목 (슬롯 기반)
  ├── 섹터 집중 제한: 동일 업종 최대 2종목
  └── 전체 투자 비중: 총 자산의 70% 이하 (30% 현금 유지)

Level 3 — 일일 Circuit Breaker
  ├── 일일 손실 -3% 초과 → 당일 모든 포지션 즉시 청산 + 매매 중단
  ├── 연속 3일 손실 → 전략 점검 모드 진입 (매매 일시 중단)
  └── 월간 MDD -10% 초과 → 비상 예비금 전환, 시스템 재검토

Level 4 — 시스템 이상 감지
  ├── API 연속 오류 3회 → 페일오버 브로커 전환
  ├── 미체결 주문 5분 초과 → 자동 취소 후 재주문
  └── 에이전트 무응답 5분 초과 → 텔레그램 긴급 알림 발송
```

### 8-2. 보안 체크리스트

```
✅ API 키 보안
   - .env 파일에만 저장 (코드/대화창 하드코딩 절대 금지)
   - .gitignore에 .env 반드시 포함
   - 출금(Withdrawal) 권한 절대 부여 금지
   - API 키는 3개월마다 갱신

✅ 서버 접근 보안
   - VPS에 SSH 키 인증만 허용 (패스워드 로그인 비활성화)
   - OpenClaw 접속 가능 IP를 개인 IP로만 제한 (화이트리스트)
   - UFW 방화벽 활성화, 필수 포트만 개방

✅ 프롬프트 인젝션 방어
   - 에이전트가 방문하는 외부 URL 화이트리스트 관리
   - 뉴스 원문 직접 처리 시 신뢰 도메인(연합뉴스, 네이버금융)만 허용
   - 텔레그램 봇 명령은 등록된 사용자 ID만 처리

✅ 모의 투자 우선 원칙
   - DRY_RUN=True 상태에서 최소 2주 검증
   - 실계좌 전환 후 첫 1개월은 전체 자금의 10%만 운용

✅ 비용 관리
   - 단순 모니터링/가격 확인: Claude Haiku 또는 Ollama 로컬 모델
   - 손익 분석/전략 판단: Claude Sonnet (1회/일)
   - 월간 재최적화: Claude Opus (1회/월)
```

---

## 13. 멀티 에이전트 팀 구성

### 9-1. 에이전트 역할 분담표

| 에이전트명 | 모델 | 작동 시간 | 주요 역할 |
|-----------|------|---------|---------|
| **오케스트레이터** | Claude Sonnet | 24/7 상시 | 전체 지휘, 크론잡 관리, 에이전트 간 조율 |
| **데이터 수집봇** | Haiku / 로컬 | 5분 간격 (장중) | OHLCV 수집, 공시 모니터링, Supabase 적재 |
| **전략 분석봇** | Claude Sonnet | 장 시작 전 1회 | 44전략 백테스트, 최적 파라미터 산출 |
| **실행봇** | Haiku / 로컬 | 실시간 (장중) | 트리거 확인, 매수/매도 주문 실행 |
| **리스크 감시봇** | Haiku | 10분 간격 (장중) | 손절 조건 확인, Circuit Breaker 감시 |
| **감성 분석봇** | Claude Sonnet | 10분 간격 (장중) | 뉴스/공시 감성 점수 → 전략 보조 신호 |
| **보고봇** | Haiku | 장 마감 후 1회 | 일일 손익 보고서 생성, 텔레그램 발송 |
| **학습봇** | Claude Opus | 장 마감 후 1회 | 손실 원인 분석, 파라미터 개선 제안 |

### 9-2. 에이전트 간 통신 프로토콜

```
오케스트레이터 ─→ 데이터 수집봇: "5분봉 OHLCV 수집 및 DB 저장"
             ─→ 전략 분석봇:  "오늘 스코어링 실행, 매수 후보 5종목 반환"
             ─→ 실행봇:       "매수 후보 종목 리스트 전달, 트리거 모니터링"
             ←─ 실행봇:       "체결 완료: 삼성전자 100주 @ 78,500원"
             ─→ 리스크봇:     "현재 포트폴리오 전달, 손절 감시 시작"
             ─→ 보고봇:       "오늘 손익 집계 후 보고서 생성"
             ─→ 학습봇:       "오늘 체결 내역 분석, 내일 파라미터 조정안 제안"
```

### 9-3. 자기학습 메모리 구조 (memory.md)

```markdown
## 전략 학습 기록 (2026-05-07 기준)

### 수익 패턴 (강화)
- 삼성전자 SMA 크로스(20/100일): 최근 3주 연속 수익, 비중 유지
- 변동성 돌파 K=0.5 → 오전 9:20~9:40 사이 돌파 시 승률 72%

### 손실 패턴 (개선)
- RSI 단독 사용 횡보장 → 볼린저 밴드 AND 조건 추가 후 개선
- 장 초반 갭 상승 3% 이상 종목 진입 → 슬리피지 과다 → 제외 필터 추가

### 파라미터 이력
- 2026-04-15: LG화학 RSI 과매도 기준 30→20으로 하향 (승률 52%→61%)
- 2026-04-22: 변동성 돌파 K계수 0.4→0.5로 상향 (휩쏘 감소)
- 2026-05-01: 일일 Circuit Breaker -3%→-2%로 강화 (MDD 개선)
```

---

## 14. 구현 로드맵 및 단계별 체크리스트

### Phase 0 — 환경 구축 (1주차)

#### 0-A. 하드웨어 준비 (비용 우선순위대로 선택)

```
[ ] 옵션 선택:
    ① 무료: Zeber VPS ($5 크레딧) — zeber.cloud 가입
    ② 저가: Hetzner CX11 ($4/월)  — hetzner.com 가입
    ③ 집 기기: 구형 노트북 Ubuntu 설치 or Mac 절전 방지 설정

[ ] Ubuntu 22.04 LTS 설치 확인
    ssh root@<서버IP>
    lsb_release -a   # Ubuntu 22.04 확인

[ ] Tailscale 보안 터널 설치 (무료)
    curl -fsSL https://tailscale.com/install.sh | sh
    sudo tailscale up   # 로그인 후 기기 등록
```

#### 0-B. 소프트웨어 설치

```
[ ] Python 3.11+ 및 기본 패키지 설치
    sudo apt update && sudo apt install python3.11 python3-pip git -y
    pip install pykrx yfinance finance-datareader requests python-dotenv

[ ] Ollama 로컬 LLM 설치 (AI 비용 절감용, RAM 4GB 이상 환경에서)
    curl -fsSL https://ollama.ai/install.sh | sh
    ollama pull gemma4       # 권장 (4GB RAM 필요)
    ollama pull llama3.2:3b  # 경량 (2GB RAM 가능)

[ ] OpenClaw 설치
    curl -fsSL https://raw.githubusercontent.com/openclaw/openclaw/main/install.sh | bash
    # Claude API 키 또는 Ollama 로컬 연결

[ ] SQLite DB 초기화 (무료, Python 내장)
    python3 -c "import sqlite3; print('SQLite OK')"
    # 또는 Supabase 무료 프로젝트 생성 (대안)
```

#### 0-C. API 발급 (모두 무료)

```
[ ] 한국투자증권 KIS API 발급
    → https://apiportal.koreainvestment.com 접속
    → 회원가입 → API Key 신청 → App Key + Secret 발급
    → 모의투자 계좌 개설 확인

[ ] 키움증권 REST API 발급 (선택 — KIS로 통일 가능)
    → https://openapi.kiwoom.com 접속
    → 영웅문 계좌 연결 → 앱 등록 → Key 발급

[ ] OpenDART API 키 발급 (무료)
    → https://opendart.fss.or.kr → 회원가입 → API 키 신청

[ ] Tavily API 키 발급 (1,000건/월 무료)
    → https://tavily.com → 회원가입 → API Key 복사

[ ] 텔레그램 봇 생성
    → @BotFather 채팅 → /newbot → 토큰 저장

[ ] .env 파일 구성 및 보안 설정
    DRY_RUN=True                    # 반드시 모의투자 모드로 시작
    KIS_APP_KEY=your_key
    KIS_APP_SECRET=your_secret
    DART_API_KEY=your_dart_key
    TAVILY_API_KEY=your_tavily_key
    TELEGRAM_BOT_TOKEN=your_token
    ANTHROPIC_API_KEY=your_claude_key   # 선택 (Ollama 사용 시 불필요)
    CLAUDE_SPEND_LIMIT=10               # 월 $10 한도 설정

[ ] Anthropic API 지출 한도 설정 (유료 사용 시 필수)
    → console.anthropic.com → Settings → Billing → Spend Limits → $10/월
```

### Phase 1 — 전략 개발 및 백테스트 (2~3주차)

```
[ ] pykrx로 국내 주식 10년치 OHLCV 데이터 수집
[ ] 퀀트 스탁 옵티마이저 엔진 구현 (quant_engine.py)
    → 44개 전략 × 주요 50종목 그리드 서치 실행
    → 샤프 지수 1.5 이상 조합 DB 저장
[ ] 래리 윌리엄스 전략 구현 (larry_williams.py)
    → 14분 레인지 계산 로직
    → ATR 기반 포지션 사이징
[ ] Streamlit 모니터링 대시보드 구현
[ ] 백테스트 결과 검토
    → 과최적화 여부 확인 (최근 1년 Out-of-sample 검증)
    → 종목별 최적 파라미터 확정
```

### Phase 2 — 모의 투자 검증 (4~6주차)

```
[ ] OpenClaw 에이전트 전략 등록 (agents.md 작성)
[ ] 크론잡 스케줄 등록 (전체 일일 스케줄 7-1 기준)
[ ] DRY_RUN=True 상태로 2주 이상 모의 투자 실행
[ ] 일일 보고서 검토:
    → 승률 50% 이상 유지 여부 확인
    → 슬리피지 / API 오류 발생 여부 확인
    → 트리거 타이밍 최적화
[ ] Circuit Breaker 및 손절 로직 정상 작동 확인
[ ] 멀티 에이전트 자기학습 루프 1사이클 완료 확인
```

### Phase 3 — 소액 실전 투입 (7~10주차)

```
[ ] DRY_RUN=False 전환
[ ] 전체 자금의 10% (예: 100만 원)로 첫 실전 운용 시작
[ ] 1주차: 슬롯 1개(1전략)만 운용
[ ] 2주차: 슬롯 3개로 확대
[ ] 3주차~: 전체 슬롯 5개 운용
[ ] 목표: 모의 대비 실전 성과 80% 이상 유지
[ ] 비용 점검: 월 AI API 비용 vs. 수익 비교
```

### Phase 4 — 고도화 및 확장 (3개월~)

```
[ ] 멀티팩터 스코어링 고도화 (감성 점수 가중치 최적화)
[ ] 섹터 로테이션 전략 추가 (업종별 순환매 포착)
[ ] 미국 주식 연동 추가 (Alpaca API — 야간 거래)
[ ] 암호화폐 연동 추가 (CCXT — 24/7 거래)
[ ] 전략 A/B 테스트 프레임워크 구축
[ ] 모델 업그레이드: Claude Opus 최신 버전 적용
```

---

## 15. 비용 구조 최종 정리 및 수익 목표

### 15-1. 3단계 비용 시나리오

#### 시나리오 A — 완전 무료 운용 (₩0/월)

```
조건: 집에 구형 노트북 보유 + Zeber VPS 무료 크레딧 활용

항목                        비용
──────────────────────────────────────
서버 (Zeber VPS $5 크레딧)  ₩0
AI 모델 (Ollama 로컬)       ₩0
주식 데이터 (pykrx)         ₩0
공시 (OpenDART)             ₩0
뉴스 (Tavily 무료 1,000건)  ₩0
DB (SQLite or Supabase 무료)₩0
알림 (텔레그램 봇)          ₩0
보안 터널 (Tailscale)       ₩0
증권사 API (KIS/키움)       ₩0
──────────────────────────────────────
합계                        ₩0/월
──────────────────────────────────────
한계: 로컬 모델 성능 제한, 하트비트 주기 최소화 필요
```

#### 시나리오 B — 최소 유료 권장 (₩10,000~₩15,000/월) ⭐ 추천

```
조건: 안정적 운용 + 소량의 Claude API 추가

항목                              비용
──────────────────────────────────────────────
서버 (Hetzner CX11, 2GB RAM)     $4/월  ₩5,500
AI 모델 — 모니터링 (Ollama 로컬) ₩0
AI 모델 — 일일 분석 (Haiku)      $2/월  ₩2,700
AI 모델 — 월간 최적화 (Sonnet)   $3/월  ₩4,100
주식·공시·뉴스 데이터             ₩0 (모두 무료)
DB (Supabase 무료 티어)           ₩0
텔레그램 + Tailscale              ₩0
──────────────────────────────────────────────
합계                              ~$9/월 ₩12,300
──────────────────────────────────────────────
장점: 안정적 24/7 운용 + 적절한 AI 분석 품질
```

#### 시나리오 C — 성능 우선 확장 (₩30,000~₩50,000/월)

```
조건: 운용 자금이 커지고 수익이 비용을 초과한 이후

항목                               비용
──────────────────────────────────────────────────
서버 (DigitalOcean 4GB RAM)        $24/월 ₩33,000
AI 모델 — Haiku (일상 작업)        $5/월  ₩6,800
AI 모델 — Sonnet (심층 분석)       $8/월  ₩11,000
AI 모델 — Opus (전략 최적화)       $5/월  ₩6,800
Supabase Pro (8GB)                  $25/월 ₩34,000
──────────────────────────────────────────────────
합계                               ~$67/월 ₩92,000
──────────────────────────────────────────────────
적용 시점: 월 수익이 ₩200,000 이상 안정화된 후
```

### 15-2. 비용 대비 손익분기점

| 시나리오 | 월 비용 | 손익분기 운용금 (월 1% 수익 기준) |
|---------|--------|--------------------------------|
| A (완전 무료) | ₩0 | 이론상 ₩1도 가능 |
| B (권장) | ₩12,300 | ₩1,230,000 이상 운용 시 커버 |
| C (성능) | ₩92,000 | ₩9,200,000 이상 운용 시 커버 |

> **결론**: 시나리오 B 기준, **300만 원 이상 운용** 시 월 수익(1~3%)이 비용을 충분히 초과한다.

### 15-3. 수익 시뮬레이션 (시나리오 B 기준, 운용금 500만 원)

| 시나리오 | 월 수익률 | 월 수익금 | 비용 차감 후 | 연 순수익 |
|---------|---------|---------|-----------|---------|
| 보수적 | +1.5% | ₩75,000 | ₩62,700 | ₩752,400 |
| 목표 | +3.0% | ₩150,000 | ₩137,700 | ₩1,652,400 |
| 낙관적 | +5.0% | ₩250,000 | ₩237,700 | ₩2,852,400 |
| Circuit Breaker 발동 | -3.0% | -₩150,000 | — | 당일 매매 중단 |

---

## 16. 핵심 원칙 및 주의사항

### 12-1. 투자 철학 5원칙

> **원칙 1: 전략 없는 AI는 빠른 손실 기계다**  
> 봇을 켜기 전에 검증된 투자 원칙을 텍스트로 명확히 정의하라.  
> AI는 그 전략을 인간보다 빠르고 규율 있게 실행하는 도구일 뿐이다.

> **원칙 2: 모의 투자는 선택이 아닌 필수다**  
> 최소 2주의 Paper Trading 검증 없이 실계좌에 투입하지 않는다.  
> 승률이 50% 미만이면 전략부터 재검토한다.

> **원칙 3: 비용 최적화가 수익률을 결정한다**  
> 단순 모니터링에 Claude Opus를 쓰면 API 비용이 수익을 잠식한다.  
> 역할에 맞는 모델을 적재적소에 사용한다.

> **원칙 4: 보안은 투자 원금 보호다**  
> API 출금 권한 부여, 키 평문 저장 = 해킹 시 전액 손실.  
> 귀찮더라도 .env 파일, IP 화이트리스트는 반드시 지킨다.

> **원칙 5: 피드백 루프 자체가 알파다**  
> 단기 수익보다 매일 손익 보고서를 통해 지속 개선하는 구조가  
> 장기적으로 시장을 이기는 진짜 경쟁력이다.

### 12-2. 하지 말아야 할 것들

```
❌ API 키를 코드에 직접 작성 (하드코딩)
❌ 출금 권한이 포함된 API 키 사용
❌ 모의 투자 없이 실계좌 바로 투입
❌ "AI가 알아서 해줄 거야" — 전략 없이 봇 구동
❌ Claude Opus를 5분마다 하트비트에 사용 (비용 폭탄)
❌ 손절 없는 매수 (무한 물타기 설정)
❌ Circuit Breaker 없는 완전 자율 운용
❌ 백테스트 결과만 보고 과거 데이터 과최적화 전략 실전 투입
```

### 12-3. 법적 주의사항

- 본 시스템은 개인 투자 목적으로만 사용
- 타인 자금의 대리 운용은 투자자문업 인가 필요 (자본시장법)
- 시세 조종, 허수 주문 등 불공정거래행위 금지 (자본시장법 176조)
- 증권사 API 이용약관 준수 (과도한 API 호출 제한 확인)

---

*본 기획서는 시스템 구축을 위한 기술 문서이며 투자 조언이 아닙니다.  
모든 투자 결정과 그 결과에 대한 책임은 투자자 본인에게 있습니다.*
